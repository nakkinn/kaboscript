# TASK：意味解析エラー検知の実装

## 難易度：低

- [x] **未定義変数の参照** — `ref_var` でテーブル検索し、見つからなければエラー報告
- [x] **重複変数定義** — `set_name_table` 時に既存エントリを検索してエラー
- [x] **未定義クラス名** — (syntax error扱いとなるが実質実装済)
- [ ] **引数の数の不一致** — `NODE_CALL` の `arg_count` と関数定義の `arg_count` を照合
- [ ] **`function` 内での `this` 使用** — `compile_varref` 時に `SEG_FIELD` かつ `FUNC` 種別ならエラー
- [ ] **インスタンスメソッドのstatic呼び出し** — `NODE_CALL` の instance 有無と関数種別を照合

## 難易度：中

- [ ] **型の不一致（代入）** — ノードに型情報を伝播させ、代入先と比較
- [ ] **型の不一致（引数）** — 引数ノードの型と実引数の型を照合
- [ ] **未定義関数の呼び出し** — クラス単位の関数テーブルを保持して検索


# その他

current_class_idの廃止